# ------------------------------------------------------------------------------
# GENERATED / ENFORCED BY STAGEFREIGHT GOVERNANCE
# Source repo: PrPlanIT/MaintenancePolicy
# Source ref: a2396c2cfee58771f6753895b0821d18d6f2bc13
# Profile: prplanit-docker
# This .stagefreight.yml is governed, not purely local.
# To disable governance, detach from the control repo workflow.
# Manual changes may be overwritten by reconciliation.
# ------------------------------------------------------------------------------

version: 1
vars:
  github_org: PrPlanIT
  github_repo: DD-UI
  gitlab_group: PrPlanIT
  org: prplanit
  repo: dd-ui
builds:
  preset: preset/build-docker.yml
scribe:
  preset: preset/scribe-docker.yml
lint:
  preset: preset/lint-generic.yml
security:
  preset: preset/security.yml
dependency:
  preset: preset/dependency.yml
commit:
  preset: preset/commit.yml
release:
  preset: preset/release.yml
manifest:
  preset: preset/manifest.yml
build_cache:
  preset: preset/build_cache.yml
ci:
  forge:
    - gitlab
  image: docker.cr.pcfae.com/prplanit/stagefreight:latest-dev
forges:
  preset: preset/forges.yml
git:
  preset: preset/git.yml
metadata:
  category: applications
  description:
    - Web UI for managing Docker hosts and their compose stacks.
    - Browser interface over Docker and compose deployments, for inspecting and operating containers across hosts.
  license: MIT
  org: PrPlanIT
  title: DD-UI
  topics:
    - docker
    - ui
    - web
    - management
orgs:
  PrPlanIT:
    aliases:
      docker: prplanit
      ghcr: prplanit
      github: PrPlanIT
      handle: prplanit
    maintainer: PrPlanIT <precisionplanit@gmail.com>
preset_source:
  cache_policy: authoritative
  project_id: PrPlanIT/MaintenancePolicy
  provider: gitlab
  ref: a2396c2cfee58771f6753895b0821d18d6f2bc13
  repo_url: https://gitlab.prplanit.com
publish:
  harbor-branch-dev:
    build: image
    kind: registry
    registry:
      - harbor
    retention:
      keep_branches: 10
      keep_last: 6
    tags:
      - test-{branch}-{sha:8}
      - latest-test-{branch}
    when:
      branches:
        - '!main'
      events:
        - push
  preset: preset/publish-docker.yml
registries:
  preset: preset/registries.yml
repos:
  github-mirror:
    forge: github
    project: PrPlanIT/dd-ui
    roles:
      - mirror
      - publish-origin
    sync:
      git: true
      releases: true
  primary:
    branches:
      default: main
    forge: gitlab
    project: PrPlanIT/dd-ui
    roles:
      - primary
    worktree: .
stencils:
  presets:
    - preset/stencils-base.yml
    - preset/stencils-status.yml
    - preset/stencils-docker.yml
test:
  preset: preset/test-go.yml
toolchains:
  preset: preset/toolchains.yml
